/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio",
    output: "export", // --> output static pages for SSG(Static Site Generation)
    reactStrictMode: true,
};

export default nextConfig;
